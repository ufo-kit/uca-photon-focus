Plugin for photon focus detectors..


## Installation

Install [libuca](https://github.com/ufo-kit/libuca) first and make sure that the
SiliconSoftware SDK is available. Then:

    mkdir build && cd build
    cmake ..
    make && make install
